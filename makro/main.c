#include <stdio.h>

#define is =
#define dude ;
#define just
#define somehow
#define magically
#define enter printf("\n");
#define show(a) printf("%2i", a)
#define wtf return 0;

int main(int argc, char const *argv[]) {
  int zahl is somehow 10;

  // Slide to zero
  while(zahl --> 0) {
    just show(zahl) dude
    magically enter
  }

  dude wtf
}
